{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Amazon Backfill Actor",
  "description": "Scrape Amazon products by ASINs",
  "type": "object",
  "properties": {
    "asins": {
      "title": "List of ASINs",
      "description": "Enter ASINs to scrape (array of strings)",
      "type": "array",
      "items": {
        "type": "string"
      },
      "editor": "json",
      "default": []
    },
    "maxAsinsPerRun": {
      "title": "Max ASINs per run",
      "description": "Maximum number of ASINs to process per run",
      "type": "integer",
      "minimum": 1,
      "default": 50
    },
    "forceRescrape": {
      "title": "Force Rescrape",
      "description": "If true, scrape ASINs even if they were scraped before",
      "type": "boolean",
      "default": false
    },
    "context": {
      "title": "Context",
      "description": "Optional context object to pass through the run",
      "type": "object",
      "editor": "json",
      "default": {}
    }
  },
  "required": ["asins"]
}
