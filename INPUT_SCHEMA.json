{
  "schemaVersion": 1,
  "title": "Amazon Backfill Actor Input",
  "description": "Input schema for the Amazon Backfill Actor",
  "type": "object",
  "properties": {
    "asins": {
      "title": "Enter Amazon ASINs",
      "description": "Paste one ASIN per line. Bulk edit supported.",
      "type": "array",
      "editor": "stringList",
      "uniqueItems": true,
      "items": {
        "type": "string"
      },
      "default": []
    },
    "maxAsinsPerRun": {
      "title": "Max ASINs Per Run",
      "description": "Maximum number of ASINs to process in a single run",
      "type": "integer",
      "editor": "number",
      "default": 50
    }
  },
  "required": ["asins"]
}